# Social Fit — Genel Analiz & Bug Raporu (2026-07-12)

Kod tabanının (backend + mobile) bağımsız denetimi. Bu oturumda **4 kritik bug düzeltildi**; geri kalanı önceliklendirilmiş backlog olarak aşağıda. Her madde `dosya:satır` kanıtıyla.

---

## ✅ Bu oturumda düzeltildi

| # | Sorun | Çözüm |
|---|---|---|
| C1 | **Koç randevusu puan farming'i** — `POST /bookings` her çağrıda +25 yıldız, dedup yok → sınırsız farm | `bookings.js`: aynı koç+saat için 409; **+25 yalnızca koç başına ilk randevuda** verilir |
| C2 | **KVKK profil sızıntısı** — `GET /users/:id` vb. tüm `Profile` alanlarını (telefon, kilo, boy, yaş) herkese döndürüyordu (gizli hesap dahil) | Yeni `backend/src/lib/publicProfile.js` (`publicProfileSelect`); posts/messages/groups/users route'larındaki `profile: true` → yalnızca public alanlar |
| H1 | **TR virgül / kalori parseFloat bug** — `parseFloat("70,5")===70`; onboarding kalori/BMI yanlış hesaplanıyordu | Yeni `mobile/src/utils/parseDecimal.js`; `OnboardingProfile`, `EditProfileScreen`, `ToolsScreen`'de uygulandı |
| H3 | **Bozuk sohbet başlığı** — profilden "Mesaj" ile açılan chat'te ad/avatar boştu (param şekli uyumsuz) | `UserProfileScreen`: ChatScreen'in beklediği `{ userId, profile }` şekli gönderiliyor |
| H2 | **Beğeni self-farm + unlike-relike loop** — kendi postunu beğen / aç-kapa-aç = sonsuz +1 | `posts.js`: kendi postuna puan/bildirim yok; beğeni geri alınınca **−1** (net-nötr) |
| H1 | **Yorum farming** — kendi postuna sınırsız yorum = sınırsız +2 | `posts.js`: +2 yalnızca **başkasının postuna İLK yorumda** |
| H5 | **PrismaClient ×15** — her dosya kendi havuzunu açıyordu → Supabase bağlantı tükenmesi riski | Yeni `src/lib/prisma.js` singleton; 15 dosya (middleware + 13 route + starService) ondan import ediyor |
| M2 | **Arkadaşlık +15 collusion** — kabul→çık→tekrar kabul = sınırsız +15 (refId=friendship.id, silinince iz kayboluyordu) | `users.js`: refId = karşı tarafın userId'si; `starTransaction`'daki kalıcı ize bakılır → çift başına **ömür boyu 1 kez** |
| H6 | **İlk paylaşım kutlaması hiç çıkmıyordu + çift-award riski** — `POST /posts` içi `recordStreak` (fire-and-forget) günü işaretleyince client'ın ayrı `/streaks/record` çağrısı hep `awarded:0` dönüyordu | `posts.js`: `recordStreak` await edilip `{awarded, bonus}` yanıtla dönüyor (foodlog deseni); `CreatePostScreen` ikinci çağrıyı bırakıp yanıttan kutluyor |
| M1b | **`PATCH /users/me` multipart string → Float/Int 500** — kilo/boy/kalori form alanından string gelince Prisma patlıyordu | `users.js`: sayıya coerce (TR virgül dahil), geçersizse 400 |
| M3 | **Gün sınırı UTC'de dönüyordu** — Render=UTC → TR'de gün 03:00'te değişiyor, 00:00–03:00 logları önceki güne yazılıyordu | Yeni `lib/dateUtils.js` — TR (UTC+3) duvar saatine göre gün anahtarı; streaks/routines/foodlog/leaderboard/streakStats'taki 5 lokal kopya tek helper'a indirildi (anahtar formatı korunduğu için mevcut kayıtlar uyumlu) |

> Backend değişiklikleri **redeploy** ister. Şema değişikliği yok — hepsi sorgu/mantık.

---

## 🔴 Backlog — HIGH (puan ekonomisi bütünlüğü & gizlilik)

- **Özel hesap okuma katmanında zorlanmıyor** — `users.js:431` (`/:id/posts`), `:447` (followers), `:459` (following) `isPublic`/arkadaşlık kontrolü yok → gizli hesap modeli baypas. → paylaşılan authorization helper.
- **Hot-path tam-tablo aggregation** — `users.js:373` her profil görüntülemede TÜM `starTransaction`'da `groupBy` (rank için); `leaderboard.js:81` benzer. Veri büyüdükçe ciddi yavaşlar. → denormalize/materialize edilmiş toplam veya periyodik job.

## 🟡 Backlog — MEDIUM

- **Upload'da dosya-tipi doğrulaması yok** — `posts.js:14` 100MB; hiçbir multer'da `fileFilter` yok; `storageService` client `contentType`'a güveniyor → public bucket'ta stored-XSS/malware + storage-DoS. → mime allowlist + magic-byte + makul limit.
- **Rate limit yalnız `/api/auth`'ta** — `index.js:57`. like/comment/booking/messages/foodlog-search (dış OpenFoodFacts proxy) korumasız. → global + mutasyon-yoğun uçlarda limiter.
- **Sayfalama yok** — `messages.js:103` tüm konuşma geçmişi, `posts.js:344` tüm yorumlar tek payload. → cursor pagination.
- **`POST /messages` — `receiverId` kontrolsüz** — `messages.js:129` sadece `notEmpty`; olmayan UUID → 500; kendine/herkese DM (blok/arkadaşlık kontrolü yok).
- **Off-design-system ekranlar (2. yeşil)** — ~14 ekran `socialFitTheme` yerine hardcoded `#2D6A4F` + sistem fontu kullanıyor (EditProfile, UserProfile, Notifications, GroupFeed, CreateGroup, EditGroup, Coaches, Blogs, More, SearchUsers, CreatePost, FoodLog, ForgotPassword, ResetPassword). İki farklı marka yeşili yan yana. → token migrasyonu.
- **Sahte/ölü UI** — Blog bölümü tamamen mock (`BlogDetailScreen` route param'ı yok sayıyor, sabit "342" beğeni); `NutritionScreen` `CalorieRing` dekoratif (dolum sabit, alıma göre değişmiyor — gerçek ring yalnız `FoodLogScreen`'de). → gerçekle bağla veya `comingSoon` arkasına al.

## 🟢 Backlog — LOW

- OTP kodu prod log'a yazılıyor (`auth.js:100`); `/me/feedback` yalnız `console.log`, persist yok (`users.js`); `posts.js:211` her post'ta tam body log.
- `CORS_ORIGINS` boşsa `origin:true` (her origin yansır) — prod env unutulursa risk (`index.js:37`).
- `foodlog.js:86` kalori `max` yok → int4 overflow → 500.
- Atomik olmayan çoklu-yazımlar (`users.js:576` çift award, `groups.js:214` grup+admin) → `$transaction`.
- Tutarsız hata zarfı (`{error}` vs `{errors:[]}`); `recipes.js:28` `next(e)` yerine düz 500.
- `leaderboard.js:9` boş DB'de sahte kullanıcılar (Alex/Sam) gerçek kullanıcıya görünür; `posts.js:330` view sayacı sınırsız/oturum kontrolsüz.
- Ölü kod: `mobile/.../ActivityCalendar.js` (hiç render edilmiyor), `BlogScreen.js` (`api.get('/blog')` — `/api` yok, 404, stack'e kayıtlı değil), `BlogDetail2`/`RecipeDetail` kullanılmayan route'lar.
- Cancel guard eksik focus/mount effect'leri (FeedScreen:213, MessagesScreen:92, ProfileScreen:105, ChatScreen:85 …) → unmount sonrası setState uyarısı.
- Debug log'lar: `CreatePostScreen.js:20/81/112`, `api/client.js:40/45`.
- Store URL placeholder'ları UI'da (`MoreScreen.js:13`, `RateUsScreen.js:10`).
- Node 18 deprecated (`@supabase/supabase-js` uyarısı) — Render/lokal runtime'ı Node 20+'ya yükselt.
- Error handler prod'da ham `e.message` sızdırıyor — `last_seen_at` incident'ında Prisma'nın iç hatası (kolon adı/sorgu detayı) client'a döndü. Prod'da generic "Bir şeyler ters gitti" + log; detay yalnız sunucu logunda.
- **Süreç dersi (2026-07-13 register kesintisi):** şema değişikliği içeren commit push'lanmadan ÖNCE `db push` uygulanmalı — Render otomatik deploy alıyor; kod kolonu beklerken DB'de yoksa tüm user sorguları 500 verir. Sıra: önce migration, sonra push/deploy.

---

## 🏗️ Mimari iyileştirme fırsatları

1. **Tek `PrismaClient` singleton** — 15 havuzu kaldırır (HIGH bağlantı riski).
2. **Star economy'yi tasarımdan idempotent yap** — `RoutineLog.awarded` / booking-once desenini beğeni/yorum/arkadaşlığa da uygula (unique constraint veya award-guard).
3. **Tek "public profile" `select`** — non-owner her yerde sadece public alanları görsün (C2 + feed/mesaj sızıntıları).
4. **Okuma katmanında `isPublic`/arkadaşlık zorlaması** — paylaşılan authorization helper.
5. **Leaderboard/rank denormalizasyonu** + global rate limit + upload `fileFilter`.
6. **Tek timezone stratejisi** — paylaşılan `toDateOnly` (streak/routine/foodlog/leaderboard).
7. **Mobile:** paylaşılan `parseDecimal` (yapıldı) + `useFocusedLoad` (cancel guard'lı) hook + off-theme ekranların token'a taşınması.
8. **Standart hata zarfı** + messages/comments cursor pagination.

---

## 📄 Bu oturumda güncellenen dokümanlar

`CLAUDE.md`, `.cursor/rules/social-fit-project.mdc`, `README.md`, `backend/README.md`, `backend/SUPABASE_SETUP.md`, `backend/.env.example`, `mobile/README.md`, `.claude|.cursor/skills/social-fit-domain/SKILL.md`, `.claude|.cursor/skills/backend-route-patterns/SKILL.md`, `.claude|.cursor/agents/backend-ui-bridge.md`, `docs/specs/tarifler.md`.

Ortak temalar: **SQLite → Supabase Postgres** (provider, `db push`, `DIRECT_URL` `sslmode=require`), **Faz 1 durumu 2/10 → 8/10**, **8 adımlı onboarding**, **routines/recipes route'ları**, `.cursor` star-puan tablosunun koddaki değerlere hizalanması.
